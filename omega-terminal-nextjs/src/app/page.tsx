"use client";

/**
 * Home Page Component
 * Main terminal interface using the modular TerminalContainer component
 * This page serves as the entry point for the Omega Terminal application
 */

import { TerminalContainer } from "@/components/Terminal";

export default function HomePage() {
  return <TerminalContainer />;
}
